# Constraint-Layered-Invariant-Verification-System
CLIVS is a "Negative Engineering" framework. Instead of simply defining what a system should do, it defines what a system is forbidden to do (Constraints).
